terraform {
  #required_version = ">= v1.1.9"

  required_providers {
    aws = {
      # source                = "hashicorp/aws"
      # version               = ">= 4.9"
      # configuration_aliases = [aws.lambda_region]
    }
  }
}